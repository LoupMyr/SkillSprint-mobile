import 'package:flutter/material.dart';

class CreateProgrammes extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Creér ton programmes"),
    );
  }
}
